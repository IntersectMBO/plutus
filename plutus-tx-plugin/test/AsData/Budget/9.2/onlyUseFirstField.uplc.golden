program
  1.1.0
  (\d ->
     (\tup ->
        (\idx ->
           (\d ->
              force
                (force ifThenElse
                   (equalsInteger 0 idx)
                   (delay (unIData d))
                   (delay error)))
             (force headList (force (force sndPair) tup)))
          (force (force fstPair) tup))
       (unConstrData d))