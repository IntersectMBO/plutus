let
  data Unit | Unit_match where
    Unit : Unit
in
\(m : integer) ->
  case
    (all dead. integer)
    (case bool (lessThanInteger m 0) [False, True])
    [ (/\dead -> m)
    , (/\dead ->
         addInteger
           (error {integer -> integer} m)
           (error {integer -> integer} m)) ]
    {all dead. dead}