(program
  (let
    (nonrec)
    (datatypebind
      (datatype
        (tyvardecl MyNewtype (type))
        
        MyNewtype_match
        (vardecl MyNewtype (fun (con integer) MyNewtype))
      )
    )
    (lam
      ds
      MyNewtype
      [
        { [ MyNewtype_match ds ] (con integer) } (lam inner (con integer) inner)
      ]
    )
  )
)