(program 0.1.0

(abs a (type) (lam xs (ifix (lam x (fun (type) (type)) (lam a (type) (all r (type) (fun r (fun (fun a (fun [x a] r)) r))))) a)
  [ { (unwrap xs) a }
    (error a)
    (lam h a (lam t (ifix (lam x (fun (type) (type)) (lam a (type) (all r (type) (fun r (fun (fun a (fun [x a] r)) r))))) a) h))]))

)
