(let (rec)
  (termbind (strict) (vardecl f (fun (con integer) (fun (con integer) (con integer))))
    (lam x (con integer)
      (lam y (con integer)
        [g x y])))
  (termbind (strict) (vardecl g (fun (con integer) (fun (con integer) (con integer))))
    h)
  (termbind (strict) (vardecl h (fun (con integer) (fun (con integer) (con integer))))
    (lam x (con integer)
      (lam y (con integer)
        [f x y])))
  f)
