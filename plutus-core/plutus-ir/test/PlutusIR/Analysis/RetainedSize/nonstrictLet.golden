(let
  (nonrec)
  (termbind
    (strict)
    (vardecl $11$ unitval (all a (type) (fun a a)))
    (abs a (type) (lam x a x))
  )
  (let
    (nonrec)
    (termbind
      (nonstrict)
      (vardecl $0$ unitunit (all a (type) (fun a a)))
      [ { unitval (all a (type) (fun a a)) } unitval ]
    )
    unitval
  )
)