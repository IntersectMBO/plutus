(let
  (nonrec)
  (termbind
    (strict) (vardecl idFun (all a (type) (fun a a))) (abs a (type) (lam x a x))
  )
  [
    { idFun (con integer) }
    [ { idFun (con integer) } [ (lam x (con integer) x) (con integer 3) ] ]
  ]
)