(let
  (nonrec)
  (termbind
    (strict) (vardecl idFun (all a (type) (fun a a))) (abs a (type) (lam x a x))
  )
  [
    (lam x (con integer) x)
    [
      [ (lam x (fun (con integer) (con integer)) x) (lam x (con integer) x) ]
      (con integer 3)
    ]
  ]
)