(let
  (nonrec)
  (termbind
    (strict) (vardecl idFun (all a (type) (fun a a))) (abs a (type) (lam x a x))
  )
  [
    [ (builtin addInteger) [ (lam x (con integer) x) (con integer 3) ] ]
    [ (lam x (con integer) x) (con integer 3) ]
  ]
)