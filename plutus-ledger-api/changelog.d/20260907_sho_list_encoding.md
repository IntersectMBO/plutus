### Changed

- Encode product types defined in the Plutus V4 ledger API as `List` instead of `Constr 0`, including their data-backed counterparts and blueprint schemas. V1-V3 encodings are unchanged.
