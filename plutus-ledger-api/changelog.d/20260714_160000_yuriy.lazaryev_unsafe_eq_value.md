### Added

- `unsafeEqValue` in `PlutusLedgerApi.V1.Value` and `PlutusLedgerApi.V1.Data.Value`: assumes both `Value`s are in the canonical ledger form (as `txInfoInputs`/`txInfoOutputs` values are), which makes it much faster than `==`. Returns a silently wrong answer if that's not the case.
