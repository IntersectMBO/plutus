# This workflow runs the cardano-constitution tests.
#
# This workflow runs on all pull requests.


name: "🗽 Cardano Constitution Tests"

on:
  workflow_dispatch: 
  pull_request:

jobs:
  run:
    name: Run
    runs-on: [self-hosted, plutus-ci]
    steps:
      - name: Checkout
        uses: actions/checkout@main

      - name: Run Tests
        run: | 
          pushd cardano-constitution
          nix run --no-warn-dirty --accept-flake-config .#cardano-constitution-test
