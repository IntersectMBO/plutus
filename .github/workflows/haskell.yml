name: Haskell CI

on:
  - pull_request
  - workflow_dispatch

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        ghc: ["ghc8107", "ghc924"]
        os: [ubuntu-latest, macos-latest]

    runs-on: ${{ matrix.os }}

    steps:
      - uses: input-output-hk/devx/actions/haskell/nix-shell@v1
        with:
          compiler: ${{ matrix.ghc }}
      - run: echo "hi"
