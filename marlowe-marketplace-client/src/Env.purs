module Env where

-- Application enviroment configuration
type Env
  -- NOTE: We currently don't need environment data, so we use this placeholder
  --       to have some value in the Env.
  = { placeholder :: String
    }
