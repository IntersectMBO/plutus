import e from"cbor";import{BlockfrostProvider as t,MeshWallet as o,Transaction as i,serializePlutusScript as r}from"@meshsdk/core";import s from"node:fs";const a=new t("Replace with Blockfrost Project ID"),n=new o({networkId:0,fetcher:a,submitter:a,key:{type:"root",bech32:s.readFileSync("seller.skey").toString().trim()}}),d=JSON.parse(s.readFileSync("./plutus-auction-validator.json")),c=r({code:e.encode(Buffer.from(d.validators[0].compiledCode,"hex")).toString("hex"),version:"V2"}).address,l=JSON.parse(s.readFileSync("./plutus-auction-minting-policy.json")),m={code:e.encode(Buffer.from(l.validators[0].compiledCode,"hex")).toString("hex"),version:"V2"},u={assetName:"TokenToBeAuctioned",assetQuantity:"1",recipient:{address:c,datum:{value:{alternative:1,fields:[]},inline:!0}}},f=(await n.getUsedAddresses())[0],p=new i({initiator:n});p.mintAsset(m,u,{data:{alternative:0,fields:[]}});const h=await p.setRequiredSigners([f]).build(),S=n.signTx(h),g=await n.submitTx(S);console.log(`Minted a token at address ${c}. Tx hash: ${g}`);