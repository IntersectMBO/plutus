module Component.$name.View
  ( render
  ) where

import Component.$name.Types.Internal (ComponentHTML, State)
import Halogen.HTML (text)

render :: forall m. State -> ComponentHTML m
render state =
  text "hello, world"
